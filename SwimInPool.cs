using System;
using UnityEngine;
using System.Collections;


public class SwimInPool : MonoBehaviour
{
		public float xSpeed = 0.01f;
		public float ySpeed = 0.01f;
	public void Update ()
	{
		Debug.Log("wtf");
		//currentPosition = gameObject.transform.position;
	
		/*while (transform.position.x >= 0.50)
		{
			transform.position.position = new Vector3(currentPosition.x -= xSpeed, transform.position.y, transform.position.z);
		}
		while (transform.position.y >= 0.07)
		{
			transform.position = new Vector3(transform.position.x, transform.position.y -= ySpeed, transform.position.z);
		}
		}*/
}

